services:
  meilisearch:
    image: getmeili/meilisearch:v1.12
    container_name: oarag-meilisearch
    ports:
      - "7700:7700"
    environment:
      MEILI_ENV: development
      MEILI_NO_ANALYTICS: "true"
      MEILI_MASTER_KEY: dev-master-key
    volumes:
      - ./meili_data:/meili_data

